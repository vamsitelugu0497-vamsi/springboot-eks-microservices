route:
  receiver: default
  group_by: ['alertname', 'service']
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 4h
receivers:
  - name: default
# Add Slack/email receiver credentials through environment/secrets in your deployment environment.
